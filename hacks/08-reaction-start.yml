# hack: processing start reaction
id: "processing start reaction"
target: plugins/platforms/telegram/adapter.py
old: |-
  if chat_id and message_id:
              await self._set_reaction(chat_id, message_id, "\\U0001f440")
new: |-
  if chat_id and message_id:
              # ATRA: a loading-style reaction while working (burning = thinking)
              await self._set_reaction(chat_id, message_id, "\\U0001f525")
verify:
  - "if chat_id and message_id:"
