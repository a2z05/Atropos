# hack: processing done success
id: "processing done success"
target: plugins/platforms/telegram/adapter.py
old: |-
  await self._set_reaction(
                  chat_id,
                  message_id,
                  "\\U0001f44d" if outcome == ProcessingOutcome.SUCCESS else "\\U0001f44e",
              )
new: |-
  await self._set_reaction(
                  chat_id,
                  message_id,
                  "\\u2705" if outcome == ProcessingOutcome.SUCCESS else "\\U0001f44e",
              )
verify:
  - "await self._set_reaction("
