[build]
builder = "nixpacks"

[deploy]
startCommand = "python atropos dashboard --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
